apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama-gemma4-e2b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama-gemma4-e2b
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ollama-gemma4-e2b
    spec:
      containers:
      - image: sysnet4admin/ollama-gemma4:e2b-it-qat
        imagePullPolicy: IfNotPresent
        name: ollama-gemma4-e2b
        ports:
        - containerPort: 11434
          protocol: TCP
      nodeSelector:
        kubernetes.io/hostname: w2-k8s
---
apiVersion: v1
kind: Service
metadata:
  name: ollama-gemma4-e2b
spec:
  ports:
  - port: 11434
    protocol: TCP
    targetPort: 11434
  selector:
    app: ollama-gemma4-e2b
